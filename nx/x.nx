

$prtcls();

i = 1;
max = 30;
$for(   $lte(i, max),
        $(  
            $loc(i, i),
            $prtcol(30 + $rand() % 8),
            $prtc(65 + $rand() % 26),

            $loc(i, max + 1 - i),
            $prtc(65 + $rand() % 26),

            i = i + 1)
);

$prtdef();